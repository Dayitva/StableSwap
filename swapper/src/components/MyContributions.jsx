import React from "react";

/**
 * @title Shows user's liquidity.
 * @description This component will show how much liquidity the user has
 * provided so far in our token pool, it will be displayed on the `Liquidity`
 * view, where we give user the ability to add more liquidity to the pool.
 */
function MyContributions() {
  return (
    <div>
      <h1>Contributions.</h1>
    </div>
  );
}

export default MyContributions;
